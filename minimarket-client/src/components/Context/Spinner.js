import React from "react";
import './Spinner.css'; // Create a CSS file to style your spinner

const Spinner = () => (
  <div className="spinner">
    <div className="loader">Loading...</div>
  </div>
);

export default Spinner;
